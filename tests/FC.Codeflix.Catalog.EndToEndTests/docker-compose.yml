version: '3.4'

services:
  fc.codeflix.catalog.fb:
    container_name: catalog-e2e
    hostname: catalog-e2e
    image: mysql
    ports:
      - "33060:3306"
    environment:
      MYSQL_ROOT_PASSWORD: root
